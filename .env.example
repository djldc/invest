# ── Neon Database ─────────────────────────────────────────
# Get this from: neon.tech → your project → Connection Details
DATABASE_URL=postgresql://user:password@hostname/dbname?sslmode=require

# ── Google OAuth ──────────────────────────────────────────
# Get this from: console.cloud.google.com
#   → APIs & Services → Credentials → Create OAuth 2.0 Client ID
#   → Application type: Web application
#   → Add Authorized JavaScript origins: http://localhost:3000
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ── Apple Sign In ─────────────────────────────────────────
# Requires Apple Developer account ($99/yr)
#   → Certificates, Identifiers & Profiles → Identifiers → App IDs
#   → Enable "Sign In with Apple"
#   → Create a Service ID and configure your domain
APPLE_CLIENT_ID=com.investingarea.web
APPLE_TEAM_ID=YOUR_TEAM_ID
APPLE_KEY_ID=YOUR_KEY_ID
# Paste the contents of the .p8 private key file (replace newlines with \n)
APPLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_KEY_CONTENT\n-----END PRIVATE KEY-----

# ── JWT Secret ────────────────────────────────────────────
# Generate a random string: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=replace-this-with-a-long-random-secret-string

# ── Stripe Payments ───────────────────────────────────────
# Get these from: dashboard.stripe.com → Developers → API keys
STRIPE_SECRET_KEY=sk_live_...
# Publishable key (safe to expose — used by Stripe.js in the browser)
STRIPE_PUBLISHABLE_KEY=pk_live_...

# Webhook secret — from: dashboard.stripe.com → Developers → Webhooks
# (For local testing, use the Stripe CLI: stripe listen --forward-to localhost:3000/api/stripe/webhook)
STRIPE_WEBHOOK_SECRET=whsec_...

# Price IDs — create in dashboard.stripe.com → Products
# Book purchase (one-time payment, e.g. $29.99)
STRIPE_PRICE_BOOK=price_...

# Premium monthly subscription (e.g. $7.99/mo)
STRIPE_PRICE_PREMIUM_MONTHLY=price_...

# Premium lifetime (one-time payment, e.g. $99)
STRIPE_PRICE_PREMIUM_LIFETIME=price_...

# ── Server ────────────────────────────────────────────────
PORT=3000
NODE_ENV=development
